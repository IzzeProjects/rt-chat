import io from 'socket.io-client'

let main = () => {
    // Import modules of a project is here
    console.log('DOM has been loaded');
    // const socket = io.connect('ws://192.168.88.253:9501', {transports: ['websocket']});
    // socket.emit('example', {data: {test: 'fdfdfdfd'}})
    // socket.on('message', function (socket) {
    //     console.log(socket)
    // })
}

export {main}
